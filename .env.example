# Optional — enables GA4 on every built HTML page via `.eleventy.js` transform.
# Create a GA4 property in Google Analytics → Admin → Data streams → Web → copy Measurement ID.
# Vercel: add GA_MEASUREMENT_ID under Project Settings → Environment Variables (Production + Preview).
GA_MEASUREMENT_ID=
